# Аватарки: план работ

> Спека: `docs/superpowers/specs/2026-08-03-avatars-design.md`.

### Задача 1. `gitspy-hosts/avatars.rs` — чистая часть

- [x] Тесты: обе формы noreply GitHub дают URL по id/логину; GitLab-форма
  даёт URL GitLab; обычная почта — `None`; регистр почты не важен
- [x] Таблица провайдеров + `service_avatar(email) -> Option<String>`
- [x] Вернуть пакетный метод `GitHub::avatars` (история коммитов → почта→URL)
  с тестами разбора — прежний код из git-истории сессии, теперь с вызывающим

### Задача 2. Хранилище и команды

- [x] `src-tauri/src/avatars.rs`: индекс `почта → {файл, время, отказ}`,
  картинки `avatars/{sha256}.img`; тесты индекса (сосед не задет, отказ
  запоминается)
- [x] Команда `avatar_paths(repo)`: почты видимой истории → пути с диска,
  служебные решаются на лету и докачиваются фоном без токена
- [x] Фоновая задача `resolve_avatars(repo)`: вычесть известное, пакетный
  метод провайдера, докачка картинок, событие `avatars:changed` (в
  `events.rs`, чтобы граница сторожила имя)
- [x] Запуск задачи после fetch/pull/push и раскрытия секции PR

### Задача 3. Фронтенд

- [x] `src/avatarCache.ts`: почта → `HTMLImageElement` (convertFileSrc),
  загрузка вне кадра, версия для перерисовки
- [x] `render.ts`: в круге узла — картинка, если готова, иначе идентикон
  (текущее поведение); тест на выбор источника чистой функцией
- [x] Подписка на `avatars:changed` → перечитать пути → bump версии

### Задача 4. Проверки

- [x] clippy, cargo test, vitest, build
- [ ] Живьём: react — аватарки ботов и людей после fetch; свой репозиторий —
  noreply-почты без токена; отключение аккаунта чистит скачанное
